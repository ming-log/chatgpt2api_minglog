你是一位顶级精通 UI/UX 设计的前端专家。请帮我重构和美化附加图片中的 Web 界面（这是一个基于 ChatGPT API 的 PPT 自动化生成后台）。

现在的页面显得有些单调、元素密集且缺乏高级感。请帮我按照【现代 SaaS 软件（如 Linear, Midjourney Web, Vercel）】的高级精致风格进行彻底的视觉升级，并输出重构后的 HTML/CSS 或 UI 建议。

请遵循以下具体的美化指导原则：

### 1. 整体风格与色彩系统 (Theme & Colors)
* **背景与网格**：移除现在的粗糙灰色背景网格，改用纯净的极简浅灰背景（如 #F8F9FA 或 #FAFBFD），或者使用极其微弱、淡化的现代点状网格（Dot Matrix）。
* **色彩搭配**：采用现代科技感配色。主色调（按钮、激活态）使用高饱和度的深邃蓝、曜石黑或钛金灰；辅助色用于状态提示（如 Online 状态使用更精致的亮翠绿脉冲呼吸灯效果）。
* **卡片与投影**：所有功能区块改用纯白卡片（#FFFFFF），搭配非常柔和、大半径的微弱阴影（如 `box-shadow: 0 4px 20px rgba(0,0,0,0.03)`），让界面呈现优雅的悬浮层级感。

### 2. 顶部导航栏 (Navbar)
* **间距与对齐**：加大顶部导航的上下内边距（Padding），让视觉呼吸感更强。
* **用户信息与状态**：将右侧的“额度、运行、ONLINE、管理员、退出”整合为一个精致的用户状态控制面板。额度与运行状态建议使用小胶囊（Badge）样式包裹，减少文字堆砌感。

### 3. 左侧控制面板 (Left Side Panel)
* **Markdown 区块**：
  * “未导入 Markdown” 的状态框过于突兀。请将其美化为一个精致的“拖拽/点击上传”虚线区域（Dashed Border），加入一个现代化的文档图标。
  * 右侧的“编辑”按钮改用更具设计感的图标按钮。
* **PPT 页数选择**：
  * “Auto / 指定” 的切换开关（Segmented Control）改用现代扁平化滑块设计，激活态使用微阴影和深色文字，未激活态淡化。
* **生成配置（文本/图片服务）**：
  * 折叠面板（Accordion）的边框和箭头要更纤细。
  * 面板内的表单项（下拉菜单、输入框）需要增加高度、加大圆角（建议 8px - 12px），并添加优雅的 Focus（聚焦）高亮动效。
  * “恢复默认”和“测试”按钮改用轻量化的次级按钮（Secondary Button）样式，避免抢夺视线。

### 4. 右侧主工作区 (Main Content Area)
* **母版风格文本框**：
  * 现在的超大文本域显得很呆板。请将其改为大圆角、带微弱灰色边框的精致输入区域。
  * 文本域下方的快捷标签（深色科技、金融咨询等）改为轻盈的圆角标签（Tag），悬浮（Hover）时有主色调的渐变或边框变色效果。
* **核心动作按钮 (CTA Button)**：
  * “生成母版”是整个页面的核心。现在全黑的方块太沉重。请将其设计为极具吸引力的现代大按钮：可以使用曜石黑带微弱渐变，或者深蓝色底色，搭配一个精致的“生成”魔法棒图标，并自带流光或平滑的悬浮放大动效。

### 5. 全局细节提升 (Global Details)
* **字体**：全局使用更具现代感的无衬线字体（如 Inter, system-ui, -apple-system, "PingFang SC"）。
* **圆角**：统一界面的几何语言，所有大卡片采用 12px-16px 圆角，按钮和输入框采用 8px 圆角。
* **间距（Spacing）**：严格遵循 8px 删格系统，加大组件之间的间距，拒绝拥挤，打造“呼吸感”和“高级留白”。

请根据以上要求，为我提供重构后的前端美化方案（包含 HTML 结构优化与 CSS/Tailwind 样式代码）。